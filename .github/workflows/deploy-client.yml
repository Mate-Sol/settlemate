name: client-CICD

on:
  push:
    branches:
      - beta # Change to your branch name if needed
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
      - run: npm i --force
        working-directory: ./client
      - run: npm run build
        working-directory: ./client

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_UNAME }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: matesol/settlemate-client

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./client
          file: ./client/Dockerfile
          cache-from: none
          push: true
          tags: matesol/settlemate-client:latest
          labels: ${{ steps.meta.outputs.labels }}

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.credmate_borrower_private_key }}

      - name: Deploy Docker container on VM
        run: |
          ssh -o StrictHostKeyChecking=no credmateborrower@34.18.114.130 << 'EOF'
            docker pull matesol/settlemate-client:latest
            docker stop settlemate-client || true
            docker rm settlemate-client || true
            docker run -d \
              --restart always \
              --name settlemate-client \
              -p 3052:80 \
              matesol/settlemate-client:latest
          EOF
